apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: statusboard-role
  namespace: statusboard
rules:
  - apiGroups: [""]
    resources: ["pods", "services", "endpoints"]
    verbs: ["get", "list", "watch"]
